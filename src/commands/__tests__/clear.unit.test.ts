// import clear from '../clear'

describe.skip('Clear command', () => {
  it('Should stop and remove active job', async () => {
    // await clear.handler()
  })
  it('Should return message if there are no active jobs', async () => {})
})
